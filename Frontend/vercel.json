{
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "framework": "vite",
    "env": {
        "VITE_API_BASE_URL": {
            "description": "Backend API base URL (e.g., https://your-backend-name.onrender.com)"
        }
    }
}