version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: resume_generator_redis
    ports:
      - "6379:6379"
    restart: always
    volumes:
      - redis_data:/data

volumes:
  redis_data:
